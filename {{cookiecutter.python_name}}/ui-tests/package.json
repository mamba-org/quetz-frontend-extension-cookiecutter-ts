{
  "name": "{{ cookiecutter.extension_name }}-ui-tests",
  "version": "1.0.0",
  "description": "JupyterLab {{ cookiecutter.extension_name }} Integration Tests",
  "private": true,
  "scripts": {
    "start": "quetz run test_quetz --copy-conf ./dev_config.toml --dev --reload",
    "test": "yarn playwright test"
  },
  "devDependencies": {
    "@jupyterlab/galata": "^4.3.0"
  }
}
